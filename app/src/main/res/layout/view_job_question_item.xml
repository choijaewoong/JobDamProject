<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="@dimen/default_dimen">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="New Text"
        android:id="@+id/text_job_question"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/image_job_question_container"
        android:textColor="@android:color/white"
        android:paddingLeft="@dimen/default_dimen"
        android:paddingTop="@dimen/default_dimen"
        android:paddingRight="@dimen/default_dimen"
        android:paddingBottom="@dimen/default_large_dimen"/>
</RelativeLayout>